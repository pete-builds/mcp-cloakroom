-r requirements.in
# Upper bound is deliberate. An unpinned ruff upgrades itself, enables a new
# rule, and reds CI with no code change in the repository.
ruff>=0.16.5,<0.17
mypy>=2.3.1,<3
pytest>=9.1.1,<10
pytest-cov>=7.1.0,<8
